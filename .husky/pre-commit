# Pre commit hook, before commit the test will meet the aceptance criteria
# The code will be linted and formatted, the code will be tested
# The code coverage will be checked

. $(dirname "$0")/scripts/check-coverage.sh
. $(dirname "$0")/scripts/check-testing.sh

# Run check coverage
check_coverage
check_testing