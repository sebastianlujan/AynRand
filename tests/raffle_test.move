#[test_only]
#[allow(unused_use)]
module aynrand::raffle_test;
    /*
    use sui::test_scenario::{Self as ts, Scenario};
    use aynrand::base_test::{Self as tb};
    use aynrand::ticket_test;


    
    //use aynrand::raffle::{Self, Raffle};
    use aynrand::ticket::{Self, AdminCap, Ticket};
    
    use std::{string::utf8};


    #[test]
    */
    /*
    fun test_buy_ticket() {

        let mut scenario = ts::begin(tb::admin());
        {   
            let admin_cap = ts::take_from_sender<AdminCap>(&scenario);
            ticket_test::test_mint_ticket(&mut scenario, &admin_cap);

            ts::return_to_sender(&scenario, admin_cap);
        };
        ts::end(scenario);
    }
    */
